# https://taskfile.dev

version: '3'

includes:
  upgrade:
    taskfile: ./.ops/taskfiles/upgrade.yaml

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
    - echo "{{.GREETING}}"
    silent: true
